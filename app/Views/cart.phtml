<div class="body" align="center">
    <div class="cart-goods body_inner" >
        <?php
        /** @var \App\Entity\CartGood $cartGood */
        foreach ($cartGoods as $cartGood):
            ?>
            <div class="cart-good">
                <div class="cart-good_image">
                    <img class="cart-good_image_inner" src="<?= $cartGood->getGood()->getImage(); ?>"
                </div>

                <div class="cart-good_name">
                    <h3><?= $cartGood->getGood()->getName(); ?></h3>
                </div>

                <div class="cart-good_color">
                    <span><?= $cartGood->getGood()->getColor(); ?></span>
                </div>

                <div class="cart-good_size">
                    <span><?= $cartGood->getGood()->getSize(); ?></span>
                </div>

                <div class="cart-good_price">
                    <h4>$<?= $cartGood->getGood()->getPrice(); ?></h4>
                </div>

                <div class="cart-good_quantity">
                    <span>количество - <?= $cartGood->getQuantity(); ?></span>
                </div>
            </div>
        <?php endforeach; ?>
    </div>
</div>

<style>
    .body_inner {
        display: flex;
        flex-direction: column;
    }

    .cart-goods {
        position: absolute;
        left: 50%;
        top: 25%;
        bottom: 0;
        transform: translate(-50%);
    }

    .cart-good {
        display: flex;
        flex-direction: row;
    }

    .cart-good_image_inner {
        height: 300px;
        width: 300px;
    }
</style>